@model Schoggifabrik.Models.ProblemViewModel
@{ ViewBag.Title = Model.Name; }

<section>
    <h2>Aufgabe</h2>
    <p>@Html.Raw(Model.Flavor)</p>
</section>

<section id="input-section">
    <h2>Input</h2>
    <p>@Html.Raw(Model.Input)</p>
</section>

<section id="output-section">
    <h2>Output</h2>
    <p>@Html.Raw(Model.Output)</p>
</section>

<div class="clearfix"></div>

<section id="code-section">
    <h2>Code</h2>
    <form id="code-form" asp-area="" asp-controller="Home" asp-action="SubmitCode" asp-route-id="@ViewContext.RouteData.Values["id"]" method="post">
        <textarea id="code" name="code">@Html.Raw(Model.StubCode)</textarea>
        <button id="code-submit" type="submit" class="button">
            <svg class="checkmark" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 52 52">
                <circle class="checkmark__circle" cx="26" cy="26" r="25" fill="none" />
                <path class="checkmark__check" fill="none" d="M14.1 27.2l7.1 7.2 16.7-16.8" />
            </svg>
            <span class="glyphicon glyphicon-send"></span>
            <span class="glyphicon glyphicon-refresh spinning"></span>
            Submit Code
        </button>
        <a id="next-problem-link" href="/" class="button" draggable="false"><span class="glyphicon glyphicon-chevron-right"></span> Zum nächsten Problem</a>
        <span id="code-submit-status"></span>
    </form>
</section>
